# Filepath Style

## Naming

### Singularization

File and directory names should be singular, not plural.
The reason for this is that we already know that directories contain multiple files and files contain multiple things.
For example, our tests are in the `test/` directory, not `tests/`.
